[project]
name = "int-interval-set"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "plotly>=6.7.0",
    "pre-commit>=4.5.1",
]
